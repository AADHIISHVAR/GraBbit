// Simplified styling - will be implemented properly later
// For now, these are placeholder functions to avoid compilation errors

pub fn card_container(_theme: &iced::Theme) -> () {
    ()
}

pub fn item_container(_theme: &iced::Theme) -> () {
    ()
}

pub fn tab_button(_is_active: bool) -> () {
    ()
}

pub fn active_button(_is_active: bool) -> () {
    ()
}

pub fn primary_button() -> () {
    ()
}
